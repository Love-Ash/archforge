{
  "expected": {},
  "notes": "negative fixture: a numeric-range en dash is legitimate typography and must pass (E2's exemption contract)",
  "generator": "python-pptx",
  "profile": "full",
  "ground_truth": "by construction (defect deliberately seeded)"
}