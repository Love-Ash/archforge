{
  "expected": {
    "E3": 1
  },
  "notes": "explicit 4pt run size via --prop size=4, below the 5pt hard floor; size set directly so no autofit or inheritance is involved",
  "generator": "officecli",
  "profile": "full",
  "ground_truth": "by construction (defect deliberately seeded)"
}
